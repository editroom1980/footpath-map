# フットパスマップメーカー 開発引き継ぎ書

最終更新時点の版：**v149** ／ 対象：`index.html`（単一ファイル）

この文書は、コードで開発を継続する人（または Claude Code）が、
現状を正確に把握し、**不具合を出さず・いつでも元に戻せる**やり方で作業を続けるためのものです。
設計の詳細は **`ARCHITECTURE.md`**（v92時点で復元済み）、回帰テストの内訳は **`footpath_regression_README.md`** を併読してください。

---

## 0. まず読む順番
1. 本書 3章「開発ルール（絶対厳守）」
2. `ARCHITECTURE.md`（描画パイプライン・不変条件・セクション地図）
3. 本書 7〜8章（回帰テストとローカル検証環境）

---

## 1. プロダクト概要
- **何**：フットパス（歩くコース）を地図上で設計する Web アプリ。スポットを置き、歩く順にルートを引き、距離・所要時間・高低差を出し、配布用画像や再編集用 JSON を書き出す。市町を問わず使える（宍粟市専用ではない）。
- **技術構成**：**単一 HTML**（HTML+CSS+インライン JS）。外部依存は CDN の **Leaflet 1.9.4** / **html2canvas 1.4.1** / **qrcodejs 1.0.0**（配布シートのQR用。読み込めなくても動作する）。地図タイルは外部提供元（OSM/GSI/OpenTopoMap 等）。
- **配信**：GitHub Pages。リポジトリ `editroom1980/footpath-map`、公開 URL `https://editroom1980.github.io/footpath-map/`。**手動アップロード**運用（後述）。
- **保存**：ブラウザの localStorage（不可時は memory フォールバック）。キーは `LS` オブジェクトに集約。
- **PWA**：ホーム画面追加で全画面起動可（icon-192/512、apple-touch-icon 同梱）。

---

## 2. 現在の状態（v149 実装済み）

| 内部名（コード・保存データ） | 画面の語（v130〜） | 以前の語 |
|---|---|---|
| `wps` / WP | スポット | ウェイポイント・WP |
| `vps` / VP | 通り道の点 | 調整点・経路調整点 |
| `customRoads` / `customPaths` / custom | 自分で描いた道 | 細道・細い道 |
| `mode==='draw'` / `enterDrawMode` | 指でなぞって描く | なぞり描き |
| `manualMode` | 道に沿わせない | 手動モード |
| `_snapOn_` / `toggleCustomSnap` | 描いた道に吸い付く | スナップ |
| `viewMode` / `body.viewing` | 歩く人の見え方 | 閲覧モード |
| type `node` | 描いた道の点 | なぞり点（調整点） |
- スポット（WP）14種、自動番号、編集（名称・種別・滞在時間・写真・電話・解説）。
- 経路：OSRM 自動ルート＋**調整点(VP)**による手動優先、ルート線ドラッグで調整点追加、手動モード（道に吸着させない）。
- 細道（手描き）＋スナップ、なぞり描き（スマホ）。
- **往復ずらし表示**（表示専用オフセット。最も反復した繊細な処理）。
- 背景地図5種：標準(OSM)/航空写真(GSI)/地形図(OpenTopoMap)/淡色(CARTO Voyager)/色違い(HOT)。PCは「地図」ボタン、スマホはメニュー。
- 地名ラベル ON/OFF、**文字サイズ5段階**（標準/大/特大/極大/最大、保存画像に反映）。○内文字は円いっぱいに中央表示。
- **大きさ設定（PC・スマホ共通／設定は保存される）**：地名ラベル5段階＋**スポット○の大きさ3段階**（標準/大/特大）。スマホは「…メニュー」、PCは「文字」ボタン。状態はグローバル共有で、再読込しても前回の設定を保持。
- 画像保存（高画質・Canvas上限内で自動クランプ）／文字なし保存。
- 高低差グラフ（PC）、閲覧モード、Undo/Redo、全消去、GPS現在地。
- コース保存（ブラウザ）／JSON書き出し・読み込み／一覧。
- **サンプルコース**：初回起動時に `sample.json` を一度だけ取り込む（写真7枚込み。削除したら復活しない）。

---

## 3. 開発ルール（絶対厳守）
このプロジェクトの最優先は「**不具合を出さない・常に元に戻せる**」。以下は例外なく守る。

1. **直接コミット/デプロイしない。** 成果物は手元に用意し、オーナーが手動で GitHub にアップロードする。アップロード後、オーナーが `?v=N` の N を1つ上げてキャッシュ更新する（`APP_VERSION` と一致していれば反映確認できる）。
2. **毎回、直前版のバックアップを併せて渡す**（`index_vNN_backup.html`）。1手で戻せる状態を常に保つ。
3. **出荷前に回帰テスト全PASSを確認**（`footpath_regression.py`）。1項目でも FAIL なら出荷しない。
4. **出力は必ず CDN 版**：`grep -c cdnjs` == 3、`grep -c 'file:///'` == 0、`leaflet-rotate` 不使用。ローカル検証用の差し替えを本番に混ぜない。
5. **静的チェックを毎回**：波括弧/丸括弧/角括弧の増減が各0、インライン JS を抽出して `node --check` が通る、`APP_VERSION` を更新。
6. **検証できない変更はしない/正直に申告**。特に html2canvas の保存画像・実機タッチ感は最終的に実機確認が必要（8章）。データ・スコア・URL を捏造しない。
7. 変更は**差分最小**。1機能=1リリースを基本に、各段階でバックアップ＋テスト。
8. （Claude が継続する場合）応答は**日本語・簡潔**。オーナーは非プログラマなので、専門用語は避け結論から。

---

## 4. 開発〜出荷の手順（毎回のフロー）
```
1. 作業前：/mnt/user-data/outputs/index.html を作業コピーへ複製（sandboxはターン間でリセットされ得る）
2. 変更を実装（差分最小・定数は先頭のCONSTANTSに集約）
3. 静的チェック：括弧均衡 / インラインJSを node --check / cdnjs==3・local==0 / APP_VERSION 更新
4. 回帰テスト：PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers python3 footpath_regression.py <file>  → 全PASS
5. 新機能があれば回帰テストに検査を追加（7章）
6. v78→v79 のように版数を上げ、直前版を index_vNN_backup.html として退避
7. present_files で index.html＋バックアップ＋（更新した）harness を渡す
8. 差分を全表示して意図した変更のみか最終確認
```

---

## 5. コード地図（`index.html`・約4800行）
上部から概ね：CONSTANTS → STATE → STORAGE → SCREEN 1（起動画面）→ GEOCODING → 背景地図 → MAP INIT → 細道 → WAYPOINTS → WPタイプピッカー → VIA POINTS → VPコンテキストメニュー → モバイルメニュー → GPS → なぞり描き → LONG PRESS → LINE DRAG → GEOMETRY → **POLYLINE（往復ずらし表示）** → OSRM ROUTING → DISTANCE+TIME → WAYPOINT LIST → MODAL → HELP MODAL → IMAGE SAVE → MODE/UNDO/CLEAR → UNDO/REDO → VP→WP変換 → 写真圧縮 → 閲覧モード → 高低差 → カスタム道路点編集/`buildCurrentSaveData` → HELPERS。

**チューニング定数（先頭 CONSTANTS に集約）**
- `DBL_GAP_PX=2` … 往復2本線の基本間隔(px)
- `OFF_GEO_M=1.0` / `OFF_MAX_PX=20` … 往復オフセットの地理基準(m)と上限(px)。**間隔調整はここ1か所**
- `IMG_SCALE=3` … 画像保存の目標倍率（`_imgScale(w,h)` が Canvas 上限内に整数クランプ）
- `ELEV_BATCH=6` … 標高APIへの同時リクエスト数（ブラウザの同時接続上限に合わせる）
- `LABEL_SIZES=[11,14,18,22,26]` / `LABEL_SIZE_LABELS` … 地名ラベルの5段階とその表示名
- `WALK_SPEEDS=[3,3.5,4,4.5,5]` / `CLIMB_MIN_PER_100M=10` … 歩く速さ(km/h)と登りの時間加算
- `WP_SIZES=[1,1.25,1.5]` / `WP_SIZE_LABELS` … スポット○の大きさ倍率（基準 スマホ36px/PC30px）。○内文字とラベル位置も追従
- `SAMPLE_URL='sample.json'` … サンプルの取得先（同じ場所に置く）
- `VERSION_URL='version.json'` … 公開中の版の置き場所（**APP_VERSION と必ず同じ値にする**）
- `LS={…}` … localStorage キーの集約（増減・改名はここだけ）
- `WT`（種別定義）/ `SYM`（記号）/ `BASEMAPS`（背景地図定義）

**知っておくと良い主要関数**
- `getCourses()/setCourses()` … コース保存の読み書き（LS.courses）
- `ensureSampleCourse()` … 初回のみサンプル取り込み（削除で復活しない／既存に触れない／失敗しても起動を妨げない）
- `loadCourseData()/renderCourseList()/buildCurrentSaveData()` … 読み込み・一覧描画・保存データ生成
- `wpIcon()/buildWpMarker()/updateTooltip()/refreshIcons()` … WPマーカーと地名ラベル
- `_wpFont(sz,sym)` … ○内文字サイズ（1文字=内側×0.72 / 2文字=×0.54。はみ出し防止）
- `_labelSize()/cycleLabelSize()/setLabelSize(i)` … 地名ラベルサイズ
- `_wpSize()/setWpSize(i)` … スポット○の直径（表示のみ。座標・ルート・高低差のデータは変えない）
- `restoreSizePrefs()/_syncSizeMenu()` … 大きさ設定の復元と、PCボタン文言・スマホメニューのチェック同期
- `setBaseMap()/cycleBaseMap()` … 背景地図切替
- `_buildDisplayCoords()` … 往復ずらしの表示座標生成（**最重要・6章**）
- `saveMapAsImage()/saveMapNoText()` … 画像保存（`scale:_imgScale(w,h)`）
- `setCourses()` … **保存できたら true / 容量超過などで書けなければ false**。`saveCourse()` はこれを見て警告し、false を返す
- `_warnRouteFallback()` … 経路サーバに繋がらず直線表示になったことを通知（30秒に1回まで）

---

## 6. 描画パイプライン（最重要・壊れやすい）
ルート線は「素の座標」と「表示用座標」を分けている。
```
経路データ → _routeLineBase（素の座標・データ準拠）
           → _buildDisplayCoords()（往復区間だけ右へずらす“表示専用”）
           → routeLine.setLatLngs(...)（画面表示のみ）
```
**絶対の前提**
- `_buildDisplayCoords` は**表示だけ**を変える。`_lastRouteCoords`・当たり判定(`hitOverlays`)・高低差・実データは**一切変更しない**。
- 往復が無ければ素の座標をそのまま返す（単線に余計なオフセットを掛けない）。
- オフセットは進行方向の**常に右法線**。行きと帰りが必ず反対側になり交差しない。折返しは半円キャップ。
- ズーム変更時は `_redrawRouteOffset`（zoomend）で再計算。
- 間隔調整は `OFF_GEO_M`（広げたいなら上げる）／上限 `OFF_MAX_PX`。

**「同じバグを再発させない」**：過去に直した自己交差・区間取り違え・間隔詰まりは、回帰テストの該当項目に対応している。緑を保つこと。詳細は `ARCHITECTURE.md`。

---

## 7. 回帰テスト（`footpath_regression.py`）
- 実行：`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers python3 footpath_regression.py /path/to/index.html`（終了コード0＝全PASS。項目数は実行時に表示、現在**431項目**（うちオフライン検査5項目はローカルHTTP配信＋圏外エミュレーションで実行））。
- 実コース（千種町千草〜岩野辺・249点）を**ハーネスに埋め込み済み**で自己完結（別 JSON 不要）。
- 検査の柱：
  - 静的：CDN参照ちょうど3・ローカルパス0・leaflet-rotate不使用・括弧均衡・JS構文・`APP_VERSION`・LSキー集約・重要関数の存在・`maxZoom:21`・各チューニング定数の維持。
  - 描画：実コースで自己交差≤1（z18-21）・往復なしは無変更・往復はオフセット・間隔が従来より狭くならない・高ズームで地理基準が効く。
  - 実行時：ロード〜描画でJSエラー無し・保存/設定のLS往復・`buildCurrentSaveData`・画像倍率が安全枠内でiPhone高画質・○内文字がはみ出さない・ラベルサイズ循環・`tooltip.update()`/新背景地図/`cycleBaseMap`/`ensureSampleCourse` の存在。
- **機能を足したら検査も足す**。`chk('区分','説明', 条件, 詳細)` を静的or機能ブロックに追記する。

---

## 8. ローカル検証環境（重要な制約と回避策）
- **Playwright**：`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`、Chromium は `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`。file:// で使う時は `--allow-file-access-from-files`。
- **Leaflet の CDN が file:// で403**：テスト時は CDNの2行（css/js）を `node_modules/leaflet/dist/…` に sed 差し替えた版で動かす（ハーネスは自動でやる）。本番出力には**絶対に混ぜない**。事前に `npm install leaflet@1.9.4`。
- **OSRM(router.project-osrm.org) が403**：実経路が要る検証は、ハーネス埋め込みの実コース座標に対して `_buildDisplayCoords` を直接当てる。ライブ・ルーティングはサンドボックスで不可。
- **地図タイルはプレビューで白くなる**：保存画像の見た目は**公開URL**で確認。html2canvas の最終レンダは実機確認が前提。
- **html2canvas は導入して部分検証できる**：`npm install html2canvas@1.4.1` し、円やツールチップを html2canvas で描画→出力Canvasの画素を解析すれば、○内文字の縦中心やラベル位置ずれを**数値で**確認できる（v78 でこの手法を使用）。
- **ヘッドレスでズーム変更**：`setMaxZoom(24)` してから**1ズームずつ別呼び出し**で `setView`（連続ループは clamp される）。
- **サンプル取り込みのテスト**：`python3 -m http.server` で配信し、`getCourses()` を評価して「初回取り込み／重複なし／既存保持／削除後に復活しない」を確認（v79 で実施）。

---

## 9. 変更履歴（v73→v79：何を・なぜ）
- **v73** 往復間隔の地理基準化（`OFF_GEO_M`/`OFF_MAX_PX`）。高ズームで線幅が頭打ちになり間隔が詰まる不満を解消。狭くならない（max演算）・交差を増やさない。
- **v74** 設定の一元化（LSキーを `LS` に集約、オフセット定数を先頭へ）。挙動不変。
- **v75** ドキュメント（`ARCHITECTURE.md`）＋未使用ヘルパ `_segKey/_ptKey` 除去＋ハーネスに実行時検査を追加。
- **v76** 画像保存の高画質化（`scale:1`→`_imgScale(w,h)`）。iPhoneの低解像度を解消、Canvas上限に整数クランプ、座標ズレ回避。
- **v77** ○内文字を円いっぱいに（`_wpFont`、2文字は縮小してはみ出し防止）＋PCに地名ラベルサイズ設定（`LABEL_SIZES`・`cycleLabelSize`、保存画像に反映）。
- **v78** ○内文字の中心ずれ修正（html2canvasがflex中央寄せを下げて描画→`text-align:center; line-height:内側`へ）＋地名ラベルの右ずれ修正（bind後のインライン変更で位置が古くなる→`tooltip.update()`）＋ラベル5段階化＋背景地図に3種追加（地形図/淡色/色違い）＋PCツールバーに「地図」切替。
- **v79** サンプルコース自動取り込み（`sample.json`・`ensureSampleCourse`）。初回のみ・削除で復活せず・既存に非干渉・失敗しても起動継続。
- **v149** **手数を減らす②：新しいコースは名前だけ・道具とカードに文字・最初の案内**。
  - 新しいコース：必須は**コース名だけ**。エリアは任意（空なら現在地→取れなければ今の地図の場所）。スタート地点・ゴール地点の欄は無くした（名前は○を押して付ける方が自然。保存データの `start/goal` は空のまま残す）。
  - スマホの下の道具（なぞる・スポット）と一覧のカードのボタン（書き出す・リンク・削除）に文字を添えた。「ファイルから読み込む（コース・GPX・発見）」。最初の案内に「赤い線を引っぱると道順が変わる」。
  - 検査3項目追加。
- **v148** **手数を減らす①：自動保存・道具の整理・スポット編集の畳み込み**（オーナー指示「わかりやすく・簡単・直感的・結果までシンプルに」）。
  - **自動保存**：変更（`_markDirty()`＝未保存フラグを立てる唯一の場所）から 1.5 秒静かなら黙って保存（`saveCourse({quiet:true})`）。「保存」ボタンは状態表示（保存済み／保存中…／保存できず）に変わり、押せば即保存もできる。一覧に戻るときも先に自動保存し、ダイアログは保存できなかったときだけ。バックアップの催促は自動保存 40 回ごと。検査中は `window.__noAutoSave` で止める。
  - **道具の整理**：スマホの下の道具は「なぞる」「スポット」だけ。「通り道の点を置く」「道を描く」は上級者向けへ（PC も「その他 › 上級者向け」）。ふだんは赤い線をドラッグすれば道順を変えられるので、専用モードは上級者向けでよい。id は据え置き（`setMode` が同じ id を見る）。
  - **スポットの編集**：種別→名前→写真→説明の順。2行目・電話・滞在時間・道順（入れる／前後を道に沿わせる）・名札の位置は「くわしい設定」に畳み、中身があるときだけ開く。文言も平易に（「このスポットを道順に入れる（外すと立ち寄らない場所）」など）。
  - 検査5項目追加（静的4・機能1）。
- **v147** **他の人による改変の可否（構想）＋ゴールの1枚（フットパス特化 F7）**。
  - 「配る › 他の人による改変」：取り込んで直してよい（標準）／そのまま使ってもらう。後者だと配布ファイル（「リンクを作る」で書き出したもの＝`shared:true`）に `noEdit:true` が入り、他の人がこのアプリの「読み込む」に入れても断る。**技術的な保護ではない**（中身は見える）。作者自身のバックアップ（`shared` 無し）は常に取り込める。
  - ゴールの1枚：歩く人がゴールに着く（帯が「ゴールに着きました」）と1回知らせ、帯を押すと記念写真のシート。撮る／選ぶ→写真の下に帯を敷いて **コース名・完歩！・日付・距離・スタンプ n/m** を焼き込み、共有（Web Share）か保存。メニュー「ゴールの記念写真」／PC「ゴール」ピルからも。
  - 検査3項目追加（静的2・機能1）。
- **v146** **配る前の確認（フットパス特化 F9）＋ WebKit の自動検査（見直し帳 C1）**。
  - 「配る」の下に **配る前の確認 n / 6**：実際に歩いた／私有地・田畑・里道に断ってある（手で✓・コースに保存 `check`）、分岐の案内／注意／トイレ・駐車場・アクセス／問い合わせ先（コースの中身から自動。押すと書く画面へ）。配ることを止めはしない（土台の確認を促すだけ）。
  - 回帰テストに **WebKit（iPhone の Safari と同じエンジン）** の検査を追加：配布リンクと編集画面を 390×812 で開き、はみ出す部品が無いこと・歩く人の帯が出ること・メニューが収まることを見る。CI にも WebKit を入れた。WebKit が無い環境では「見送り」として通す。
  - 検査4項目追加（静的1・機能1・WebKit 2）。
- **v145** **地図の色（テーマ）**（構想「色を自由に」の端末内版）。「地図の見せ方 › 地図の色（テーマ）」（PC・スマホ、編集のときだけ）。組み合わせ5つ（標準／里山／海と空／秋／白黒コピー向け）＋道順の線の色8色＋点線／実線＋太さ（細・標準・太）。選ぶとその場で地図に映り、「この色にする」でコースに保存（`theme`）。歩く人の画面・配布シート（凡例の線も）・地図画像が同じ色になる。標準のときは保存に書かない。
  - スマホのメニューを1画面に収めるため、「写真をシールで見せる」と「地図の色」は2階層目「見た目（色・シール・文字の大きさ）」へ（PC は「標準地図 ›」の中）。
  - 実装：`applyTheme()` が `WT[].c`（印の色の唯一の出どころ）と `LINE_STYLE.color` を書き換え、印・名札・一覧・線・三角・凡例を描き直す。読み込み時は印を作る前に静かに適用。コースを閉じると標準に戻す。アプリの操作部分（ボタン等）の色は変えない。
  - 検査3項目追加（静的2・機能1）。基準画像は変わらない（標準のまま）。
- **v144** **周辺の情報を取り込む（構想 D）**。PC「その他 › 周辺の情報を取り込む」／スマホ「コース › 周辺の情報を取り込む」（編集のときだけ）。**OpenStreetMap（Overpass API・2系統）と Wikipedia（ja・geosearch＋冒頭2文）** から、コース（道順の線かスポット）から 300m／1km／2km 以内の候補を種類（お店・飲食／神社・寺院／史跡・見どころ／展望／公園・自然／学校・幼稚園／公民館・役所／トイレ／駐車場／バス停・駅／Wikipedia）で探す。**Google の情報は規約で Google 以外の地図に載せられないので使わない**。
  - 候補は一覧（✓で選ぶ・全部選ぶ／外す）と地図の薄い○（押すと選択）。既にあるスポット（15m 以内で同名、または 5m 以内）は除く。選んだものだけを **道順に入らないスポット**（種別は自動、説明に営業時間・電話・出典「（情報：OpenStreetMap）」「（Wikipedia「題名」より）」）としてまとめて置く。取り消し1回で全部戻る。
  - 国土数値情報（学校・バス停・公共施設・観光資源・文化財）は県別ファイルの事前変換と自前配信が要るので今回は入れていない（次の候補）。
  - 検査4項目追加（静的3・機能1、通信は差し替えて検査）。基準画像は変わらない。
- **v143** **発見（歩く人が見つけたものを貼る・作者に送る・作者が取り込む）**。構想「参加者が発見した植物・マンホールの蓋…を共有」の、サーバ無し・0円版。
  - 歩く人の画面（配布リンク／歩く人の見え方）に 📷（スマホは右列・PCは「発見」ピル）。写真（撮る／選ぶ）＋ひとこと30字＋名前12字。場所は直近2分以内の現在地、無ければ取り直し、それも無ければ地図の中心。印は緑の「発」つきシール、長押しで動かせる。押すとカード（消す・作者に送る）。
  - 端末の `LS.finds`（コースIDごと）と IndexedDB（写真）に残る。コースの中身は変えない。編集画面では出さない。片づけ `gcPhotos` は発見と分岐の写真も残す。
  - 「見つけたもの（発見）」（メニューの行／PCピル）：一覧・全部消す・**作者に送る**＝`fpFinds` ファイルを Web Share（iPhone：LINE・メール・AirDrop）、できなければ保存。iPhone の共有は押した直後に呼ばないと開けないので、シートを開いた時点でファイルを作っておく。
  - 作者側：一覧画面の「読み込む」に発見ファイルを入れると、同じコース（ID→名前で照合）に **「その他」の道順に入らないスポット** として取り込む（確認つき・写真3枚まで）。開いて種類や場所を整える運用。
  - 検査6項目追加（静的5・機能1）。基準画像は変わらない。
- **v142** **写真を軽く多く・シール（構想 A の1つ目）**。保存する写真は長辺 1024px・JPEG 0.66（従来 800px・0.65）。描き直して保存するので EXIF（撮影場所など）は残らない。1スポット **12枚**まで（従来5枚）。
  - **シール**：「地図の見せ方 › 写真をシールで見せる」を ON にすると、写真のあるスポットの印が写真そのもの（角丸・左上に種別の小さな○・番号）になる。設定はコースに保存（`stickers`）され、歩く人の画面・配布シート・地図画像にも同じに出る。シールは1枚目の写真を 96px 角に切り出し、参照写真のものは `s:`+id で IndexedDB に置く（片づけ `gcPhotos` は消さない）。
  - 近いシール（46px 以内）は先頭の1つに **+n** を付けて束ね、残りは隠す。押すと2段寄って開く。○の印は束ねない。
  - スポット一覧に写真の枚数（📷n）。検査5項目追加（静的4・機能1）。基準画像は変わらない（検査コースはシール OFF）。
- **v141** **分岐と注意の案内（フットパス特化 F4／F11）**。通り道の点に「分岐（←↑→）」か「注意（車が多い・滑りやすい・携帯が圏外・獣に注意・その他）」の案内を付けられる（`vp.guide`：ひとこと40字と写真1枚。点のメニュー「分岐・注意の案内を付ける」）。印は矢印（橙）／「!」（黄土）。写真はスポットと同じく IndexedDB へ退避し、配布ファイルには実体を同梱。
  - 歩く人の帯：次のスポットより手前で **120m から「100m先 左：橋を渡ってすぐ」**、20m以内で **「ここを左」**、25m過ぎたら **「✓ この道で合っています」**（注意は「この先100m 車が多い」）。帯を押すと案内のカード（写真）。閲覧中に点を押すと編集メニューではなく案内が出る（閲覧中の門を1つ塞いだ）。
  - 配布シートに「分岐・注意の案内（歩く順）」（小さな写真つき）。地図画像（配布シート・画像で保存）にも矢印／「!」の印だけは残す（通り道の点そのものは従来どおり映さない）。
  - 検査5項目追加（静的4・機能1）。基準画像は変わらない（検査コースに案内なし）。
- **v140** **見直し帳の小物5つ**：B1 スポットの削除にも10秒の「元に戻す」（確認ダイアログをやめる。戻すのはスナップショットの取消なので、その間に別の操作をしていたら「取消ボタンで」と案内）／B3「配る」の写真つきスポットを押すと写真の無いスポットの編集画面へ／B4 並べ替えの件数から「描いた道の点」を除き、点の行は控えめに／B7 種別チップの畳んだ側をこのコースで使った順に／C3 通知（トースト）を1つの箱に積み、重ならない（3つまで）。
  - C5（文字なしボタンの検査をスマホにも）は v122 の検査が既に全画面のボタンを数えているので追加なし。C2（実機テストの手順）は下の「実機テスト」節に。検査6項目追加。

### 実機テスト（1回15分・版が上がるたびに）
| 項目 | 手順 | 合格 |
|---|---|---|
| 拡大 | 地図の外（帯・ボタン）を二本指で広げる／二度押し | 画面が拡大しない（v135） |
| コンパス | 配布リンク→◎→🧭→許可 | 体の向きに合わせて矢印が回る（v132） |
| 読み上げ | ○のカードの🔊／心得の🔊 | 日本語で読み、もう一度押すと止まる（v134/v137） |
| 圏外 | Wi-Fiで配布リンクを開いて30秒待つ→機内モード→開き直す | 地図とコースが出る（v135） |
| 置く | 地図を1回タップ | すぐコースポイントが置かれ、案内が1回出る（v128） |
| 分岐の帯 | 分岐の案内を付けたコースの配布リンク→◎で歩く | 手前で「100m先 左」、そこで「ここを左」、過ぎて「✓ この道で合っています」（v141） |
| 発見 | 配布リンク→右列の📷→「撮る」 | カメラが直接開き、貼ると緑「発」のシールが出る。長押しで動く（v143） |
| 発見を送る | メニュー「見つけたもの」→作者に送る | 共有シート（LINE・メール・AirDrop）が出て、ファイルが送れる（v143） |
| シール | 「見た目 › 写真をシールで見せる」ON→保存→配布リンク | 写真の印が出て、近いものは +n で束なり、押すと寄る（v142） |
| 地図の色 | 「見た目 › 地図の色」で里山→保存→配布リンク・配布シート | 線と印の色が変わっている（v145） |
| ゴールの1枚 | 配布リンクで◎→ゴール付近まで歩く（または帯が「ゴールに着きました」）→帯を押す | 撮った写真にコース名・日付・スタンプが入り、共有シートから LINE 等へ送れる（v147） |
- **v139** **協会式のコース情報（フットパス特化 F2）**。各地のフットパスマップに共通する7項目（アクセス／車・駐車場／トイレ／休憩・おもてなし／おすすめの季節／注意／問い合わせ先）と難易度★（自動＝距離と登り、または手で★1〜3）を、`courseInfo.info`（保存データ `info`）に持つ。
  - 書く：メニュー「コース › コースの情報」／PCの左の欄（難易度の下のボタン）／「配る」の載る情報（n/7 記入）。読む：歩く人はメニューの「コースの情報」→「このコースについて」。
  - 配布シート：右欄「コースの情報」（書いた項目だけ）、見出しの難易度に ★★☆。
  - 配布シートの基準画像を作り直した。検査4項目追加。
- **v138** **ゆっくり基準と区間の目安（フットパス特化 F3）＋歩く人の帯の仕上げ（A1〜A4）**。歩く速さの既定を **3.0km/h「フットパス・立ち止まる前提」** に（各地のマップの実測は2.5〜3km/h。既に速さを選んで保存した人はそのまま）。配布シートに **区間の目安（S→① 12分…）** の行（コース沿いの距離÷速さ。登りと滞在は含めない）。
  - 帯：**押すと次のスポットのカード**（A1）／**25m以内で「到着：③ ○○」＋次**（A2）／**位置の精度が40mより悪いと「約」**、30秒より古い位置は薄く（A3）／`aria-live`（A4）／距離の横に「約○分」。
  - 配布シートの基準画像を作り直した（区間の目安の行）。検査4項目追加。
- **v137** **歩く人の心得を標準搭載（フットパス特化 F1）＋PCの歩く人に「一覧」を出さない（A5）**。日本フットパス協会の3か条（田畑や私有地には立ち入らない／ゴミは必ず持ち帰る／動植物や農作物を採らない）を土台に、英国 Countryside Code の3見出し（配慮する・守る・楽しむ）で `KOKOROE` に定義。コースごとの「この地域からのお願い」（`courseInfo.kokoroe`・保存データ `kokoroe`）を作る人が書ける。
  - 出る場所：**配布シートの下段**（`.sh-kokoroe`）／**歩く人の最初の案内**（要点1行＋「心得を読む」）／**心得のシート**（`#kokoroeSheet`：歩く人は読む＋🔊読み上げ、作る人は追記を書く）／メニュー「コース › 歩く人の心得」／PCの下の帯「🙏 心得」／「配る」の載る情報に「標準の文／標準＋この地域の追記」。
  - 読み上げを `_speakText()` に共通化（スポットのカードと心得で同じ）。
  - A5：`body.viewonly .hbtn-back` を隠す（配布リンクをPCで開いた人が作る人の一覧に迷い込んでいた）。
  - 配布シートの基準画像を作り直した（心得の段が増えた）。検査6項目追加（静的5・機能1）。
- **v136** **見直しで見つけた3件の修正**（Chromium・WebKit × iPhone 2幅・PC 2幅で26状態を自動点検＋コードの読み直し。コンソールのエラーは0）。
  - 長い通知（トースト）が375px幅で両側にはみ出していた → 折り返す（`whiteSpace:normal`・`maxWidth:calc(100vw - 24px)`）。
  - コース削除の「元に戻す」を10秒以内に続けて2回使うと、前のトーストのタイマーが新しい方を消して戻せなくなっていた → 前のタイマーを止めてから出す。
  - 配布リンクの地図の自動保存（v135）で、縮尺が10のとき1枚も保存しなかった（ループの下限が11） → 10でも保存。
  - 検査4項目追加（静的3・機能1）。配布シートは縦にスクロールする画面なので、画面下に続くのは正常（点検の誤検知として除外）。
- **v135** **iPhone でボタンが画面からはみ出す不具合を修正（オーナー報告・最優先）**。原因は v122 で `user-scalable=no` を外したこと。iPhone のホーム画面から開くアプリではこの指定が効くため、地図の外（下の帯・上のボタン）を二本指で触るとページごと拡大され、右側の保存・ボタン列・「…」が画面の外へ出ていた（Safari のブラウザ表示では元々無視される指定なので、Playwright でも WebKit でも再現せず、実機の報告で分かった）。
  - 直し：`maximum-scale=1.0, user-scalable=no` を戻す（文字の大きさはアプリ内の設定で変える）。ダブルタップ拡大も `html,body{touch-action:manipulation}` で止める。ブラウザ表示でピンチされた場合は `_unzoomPage()` が viewport を入れ直して 1.0 に戻す。
  - 再発防止の検査：iPhone の幅（402・375）で見えている要素が画面からはみ出さない（毎回自動で数える）。v122 の「拡大を禁止しない」の検査と設計の決めごとは、この理由で反転させた（ARCHITECTURE 12）。
  - **実機で確認してほしい**：ホーム画面のアプリを一度閉じて開き直し、ピンチしても画面が拡大しないこと。もし今も拡大されたままなら、アプリを閉じて開き直すと戻る。
- **v135（同版）** **圏外でも配布リンクが開く（ロードマップ 段階2-3）**。配布リンク（`?course=…json`）を開くと、道順・標高が落ち着いた4秒後に、**コース範囲の地図タイルを自動で持ち歩く**（`_autoOfflineForLink`）。作る人がメニューから押す必要が無くなった。コースのJSON自体は以前から `sw.js` のネット優先キャッシュに入る。
  - 上限：Wi-Fi と分かれば1,200枚、分からなければ500枚（iPhone は回線の種類が取れない）。モバイル回線・通信節約モードと分かれば見送る。埋め込みでは見送る。コースごとに1回（`LS.offAuto`）。広いコースは縮尺を落として収める。取得は4枚ずつ（提供元への配慮）。終わったら1回だけ知らせる。
  - `?offauto=0` で見送る（検査用。`?nosw=1` と同じ発想）。
  - 検査4項目追加（静的2・機能1・圏外1：**配布リンクをオンラインで開いてから圏外で開き直してもコースと道順が出る**＝既存の圏外検査を配布リンクにも広げた）。
  - 正直な限界：圏外で見えるのは持ち歩いた範囲・縮尺だけ。回線の種類が分からない端末では500枚（およそ2縮尺ぶん）まで。
- **v134** **解説の読み上げ（ロードマップ 段階2-2）**。歩く人の画面でスポットの○を押したカードに「🔊 読み上げ」。端末の音声合成（Web Speech `speechSynthesis`）で名前＋解説を日本語で読む。サーバも通信も要らない。もう一度押すか、カードを閉じると止まる。日本語の声があればそれを選ぶ。音声合成が無い端末ではボタンを出さない。
  - **実機未確認**（Playwright では音が出ないため、音声を差し替えて呼び出しを数える検査）。iPhone・Android で実際に読めるかは実機で。
  - 検査3項目追加（静的2・機能1）。
- **v133** **高低差グラフに「いまここ」（ロードマップ 段階2-4）**。歩く人の位置（v132 の `_onWalkerPos`）が入ると、下の帯のグラフにコース沿いの進みで緑の丸を出す（`_elevHerePoint`：進み(m)→横位置、標高は標本点の間で補間）。展開時は「いまここ」の文字も。位置が無いときは出さない。
  - スタンプの札を、2行になった「次のスポット」の帯の下へ（v132 で端が触れていた）。
  - **往復コースの取り違え対策**（検査で発覚）：同じ道を2回通ると、位置1つに「行き」「帰り」の2つの進みがある。`_routeCandidates` が候補を進みで束ね、`_routeProgress` は前回の進みに近い（できれば前へ進んだ）ものを選ぶ（歩く人は連続して動く前提。`ROUTE_AMBIG_M/ROUTE_CLUSTER_M/ROUTE_BACK_M`）。スポットも行き・帰りの両方で「次」になる。**正直な限界**：位置が大きく飛ぶと（電源を切って別の場所で入れる等）行き帰りを取り違えることがある。
  - 検査4項目追加（静的3・機能1：点を順にたどって 30%→60% で丸が右へ動き、進みが 30%・60% と合う。位置が無ければ出ない）。
- **v132** **次のスポットまでの距離と向き（ロードマップ 段階2-1）**。歩く人の画面（配布リンク・歩く人の見え方）の上に「次：③ 加茂神明神社 まで 180m 北東」の帯（`#nextBar`）。位置は **◎（歩く人の画面では追いかける動作に変更）か追従** から `_onWalkerPos()` に入る（入口は1つ）。
  - 「次」の決め方：いまの位置をコース線に投影した進み（`_routeProgress`）より `NEXT_MIN_AHEAD_M`（15m）以上先にある最初のスポット。通り過ぎたスポットは自動で外れる。距離はコース沿い、コースから `NEXT_OFFROUTE_M`（60m）以上離れていれば直線距離に「（直線）」。ゴールを過ぎると「ゴールに着きました」。
  - 向き：北=0の矢印＋方角の文字（8方位）。🧭を押すとコンパスの許可を求め（iPhone は押したときだけ求められる）、取れたら矢印が体の向き基準になる。**実機のコンパスは未確認**（Playwright では取れない）。
  - 歩く人の画面では「歩く人の見え方」の札（`#viewBadge`）を出さない（歩く人には意味が無く、帯と重なるため）。埋め込みでは帯を出さない。
  - 検査5項目追加（静的4・機能1：位置を差し替えると距離が更新され、ゴールで文言が変わり、コンパス向きで矢印が回る）。
- **v131** **PC・スマホの機能を揃える（ロードマップ 段階1-6）**。PCの左の道具に **「なぞる」（指でなぞって描く＝`setMode('draw')`）と「道を描く」（自分で描いた道＝`toggleCustomMode()`）**、下の帯に **「現在地」**、左の欄に **「並べ替え」ボタン**（並べ替え画面はPCでは中央に出す）、「その他 › 上級者向け」に **「自分で描いた道を使う」「描いた道に吸い付く」**。描く機能はもともとマウスのイベントも受けていたので入口を足しただけ（実データでマウスのドラッグで描けることを確認）。
  - **スマホの「高低差の詳細」は既に帯を上に開くと出ていた**（最高・最低・上り・下り＋目盛りつきのグラフ170px）。コードは足さず、検査で固定した。
  - ついでの不具合修正：PCの「高低差」が最初の1回で開かなかった（`toggleElevPanel` がインラインstyleの空を「開いている」と誤判定）。実際の表示で判定するように。
  - 検査5項目追加（機種だけで使えない機能が0＝22機能の入口をPC側・スマホ側の両方で数える／PCで4機能が動く／高低差が1回で開く／スマホの帯の詳細）。地図を持ち歩く・現在地追従はスマホ専用のまま（ロードマップどおり）。
- **v130** **言葉の言い換えと、コース削除の「元に戻す」（ロードマップ 段階1-5）**。画面に出る語を統一：**調整点→通り道の点／細道→自分で描いた道／なぞり→指でなぞって描く／手動モード→道に沿わせない／スナップ→描いた道に吸い付く／ウェイポイント・WP→スポット／閲覧モード→歩く人の見え方**。ボタン・メニュー・トースト・確認ダイアログ・操作ガイド・種別「描いた道の点」まで。**内部名（`vps`・`customRoads`・`manualMode`・`viewMode`・保存データのキー）は変えていない**（下の対応表）。操作ガイドは今の画面（配る・その他・通り道）に合わせて書き直した。
  - **コースの削除**：確認ダイアログをやめ、すぐ消して10秒間「元に戻す」（`#undoToast`）を出す。押すと同じ位置・同じ中身で戻る。10秒たつと確定（`DELETE_UNDO_MS`）。別のコースを続けて消すと前のは確定。
  - 検査5項目追加（旧語が残っていない／新語がある／ガイドが今の語／削除は元に戻せる）。合格基準の「説明なしで通じるか」は**利用者テスト（オーナー側）で確認が必要**。
- **v129** **種別の追加と「よく使う4つ」の入れ替え（オーナー指摘）**。編集画面で大きく出す4つを **コースポイント・ビュースポット・史跡・記念碑・飲食店・ショップ** に（トイレはそんなに頻繁に置かない、史跡・記念碑と飲食店のほうが多い）。神社・寺院・トイレは「その他の種類」へ。
  - 種別に **学校・幼稚園（○に「学」・#007A85）** と **公民館・集会所（○に「公」・#3E2723）** を追加（12→14種）。色は既存12色と、色覚シミュレーション（1型・2型）込みで最も離れた組を計算で選んだ（学校は一番近いトイレとΔE 14.8、公民館は調整点の灰色と12.8。白文字の読みやすさは 5.09:1／13.8:1）。凡例・配布シート・GPXの `<type>`・スタンプ対象はすべて `WT`／`SYM` を見るので自動で対応。
  - 検査1項目追加（新しい2種と記号）。既存の色の検査（重複なし・4.5:1以上）も新色を含めて通過。
- **v128** **スポットを置くのを1タップに（ロードマップ 段階1-4）**。地図をタップすると、種類の選択画面も編集画面も開かずに、まず「コースポイント」として置く（`onMapClick` → `addWp(…,'course')`）。種類と名前は○を押してあとから。**10個置くのに必要な画面切替が20回→0回**（検査で確認）。最初の2回だけ「置きました。○を押すと種類と名前を変えられます」と知らせる。
  - 編集画面の種別は select（隠す・値の置き場）＋チップ（`#mTypeChips`）。**よく使う4つ（コースポイント・ビュースポット・神社・寺院・トイレ＝`TYPE_BIG`）を大きく、残りは「その他の種類（n）」に畳む**。いまの種類が畳んだ側なら開いた状態で出す。選べる種類の出どころは `_buildTypeOptions`（スタート／ゴールは両端の点だけ）のままで、チップは select の中身を読むだけ。
  - 現在地マーカーをタップして置くときは、これまでどおり種類の選択画面（`showWpTypePicker`）。最初の案内（v107）の文を「種類と名前は、○を押してあとから決められます」に。
  - 検査5項目追加（静的4・機能1）。実機で見てほしい点：置いた直後の案内（トースト）が邪魔でないか、4つの大きいチップが指で押しやすいか。
- **v127** **開くのを速く（オーナー報告「地図の読み込みが遅い」）**。本番で計測すると、ページ自体は速い（DOMContentLoaded 0.2秒・タイル1枚80〜130ms）が、**コースを開くたびに経路サーバ20回（約3秒）＋標高サーバ63回（合計6.3秒）**を問い合わせていた。v119 の同梱はサンプルと v119 より前のコースには効いていなかった。
  - **標高も同梱**：点ごとの標高キャッシュ `elevCache`（キー `_elevKey`＝小数5桁の緯度経度）を保存データの `elevs` に入れ、読み込みで戻す。`_fetchElevs` はキャッシュにある点を取りに行かない。標高が揃っていれば経路確定後の2秒待ちも省く。
  - **静かな書き足し** `_persistDerivedQuietly()`：計算し終えた道順（`routes`）と標高（`elevs`）を、保存操作なしで保存データに書き足す。触るのはこの2項目だけ（名前・スポット・説明・`savedAt`・未保存フラグ・バックアップの回数は変えない）。未保存の編集がある間は書かない（保存のときに一緒に入る）。これで **v119 より前に保存したコースも、2回目からは問い合わせ0で開く**。
  - **サンプルコース（`sample.json`）に道順20区間・標高63点を同梱**（実際に計算した結果。+5.8KB）。初めての人が最初に開くコースが即時になる。
  - 書き出し専用の html2canvas・qrcodejs は `defer` で後回し（起動を待たせない。Leaflet は先のまま）。
  - 実測（localhost・サンプル）：開いてから道なりの経路＋高低差まで **6.0秒 → 問い合わせ0で即時**。検査7項目追加（同じ点を二度取らない／同梱を読めば0回／書き足しは routes・elevs だけで未保存中は書かない／サンプルに同梱／defer）。
  - 正直な限界：経路サーバ・標高サーバの速さそのものは変えられない。**初めて開くコース（道順を作る時）は今までどおり計算する**。同時実行数（4）は提供元への配慮で据え置き。
- **v126** **PCの道具を役割で3群に（ロードマップ 段階1-3／図4／UI点検 01・09・12・14）**。16個が横一列（検査幅では2段に折り返していた）のツールバーを無くし、**上バー＝一覧・コース名・歩く人の見え方・その他・保存・配る／地図の左＝スポット・通り道｜取消・やり直し（縦の道具 `#tbar`）／右上＝背景地図（5種＋地名とスポット・文字と印の大きさ）と凡例／下＝高低差** に分けた。ボタンはすべて文字つき（文字なし0・同じ文字0）、上バーは1024px幅でも1行。
  - 「その他」（`#popMore`）：JSONで保存・座標を書き出す（調査用）・文字なしで画像保存・操作ガイド／上級者向け＝手動モード（道に沿わせない）・通り道の点を表示／すべて消去。GPX・配布シート・画像保存は v123 の「配る」に一本化。
  - 凡例（`#popLegend`）は配布シートの `_sheetLegend()` をそのまま使う（出どころ1つ）。背景地図の名前・✓・スイッチ・大きさは `_syncPcPops()` が状態の変数から描き直す。
  - **id は据え置き**（`btnWp/btnVia/btnUndo/btnRedo/btnBaseMap/btnElev/btnView/btnManual/btnToggleVia/btnMapLabels/btnLabelSize/lblSizeTxt/tbar-st`）。既存の同期コード（`setMode`・`toggleManualMode`・`openElevModal` など）は触っていない。`#tbar` の id も残したので、閲覧中・配布リンクで隠す規則がそのまま効く。
  - 上バーは白地（図4）。閲覧中は「歩く人の見え方」が「編集にもどる」になり、左の道具と案内は消える（右上の背景地図・凡例・下の高低差は残す）。
  - **保存画像の基準（`tests/baseline/map_image.png`）を作り直した**。旧ツールバーが検査幅で2段に折り返していた分だけ地図が高くなり（2049→2244px）、中身は同じことを目視で確認。
  - 検査7項目追加（静的6・機能1：1024pxで文字なしボタン0／同じ文字0／上バー60px以下／道具が地図の中／背景地図→航空写真でピルの文字が変わる／凡例に行がある／その他7行・手動が切り替わる／閲覧中に道具が消える）。旧検査5項目は前提が変わったので意図を保って書き換え（文字サイズ段階の数え方をスマホメニュー内に限定、GPX・配布シートは「配る」経由、「調整点」文言→「通り道」）。
  - **まだやっていないこと（図4の残り）**：左の欄の「スポット／コースの説明」タブと写真の有無の印、道を描く道具（PCで細道を描く手段は 1-6 で揃える）、高低差を「なぞると地図が光る」帯にすること。
- **v125** **スマホのメニューを1画面に（ロードマップ 段階1-2／UI点検 10）**。以前は7区画・30項目で高さ1480px（390×844 の画面の約2倍）。順番を **コース → 歩くとき → 地図の見せ方 → 上級者向け（畳む）** に組み替え、背景地図・文字と印の大きさ・歩く速さは「›」で開く2階層目（`#mmSub`）へ、書き出し（画像・配布シート・GPX）は v123 の「配る」へ寄せた。開くたびに1階層目・畳んだ状態から始まる。
  - 「›」の右に今の設定（背景地図名・大きさ・速さ・並べ替えの件数）を出す（`_syncMmValues()`）。開かなくても分かる。
  - 文字の大きさは **標準／大／特大の3段階だけ見せる**。以前の極大・最大は、選んである人の行だけ出す（設定は消さない。`LABEL_SIZES` は5のまま）。
  - 上級者向け（畳む）：調整点を表示／手動モード（道に沿わせない）／細道機能（自分で描いた道）／スナップ（描いた道に吸い付く）／文字なし保存・JSON書き出し・ルート座標・ヘルプ／すべて消去。言い換えは 1-5 で全画面そろえるまで「いまの語＋説明」の併記。
  - 選択肢の行（`data-bm`／`data-lsz`／`data-wsz`／`data-spd`）は以前の行をそのまま移しただけなので、同期関数は変えていない。`data-edit="1"` の7か所も同じ。
  - 検査6項目追加（静的5・機能1：390×844 で `scrollHeight ≤ clientHeight`、2階層目の往復、3段階、畳む）。実機で見てほしい点：2階層目の「‹ 戻る」の位置が親指で届くか。
- **v124** **スマホの棚の「…」が枠からはみ出す不具合を修正**（オーナー報告・iPhone）。棚（`#mobileShelf`）の中身がどれも縮まない作りだったため、375px幅（iPhone 13 mini など）では所要時間が「49分」でも残り0px、**1時間を超える（「1時間25分」）と35px、320〜360px幅では実データでも**「…」が棚の外に出ていた。
  - 直し：距離時間（`.mob-stats`）だけを縮む側（`flex:1 1 auto;min-width:0`）にし、入りきらない時だけ2行に折り返す（375px幅・「12.8km 1時間25分」で2行、実データは1行）。399px以下は折り返した時に「·」だけ残らないよう区切りを消す。**モードボタンは44pxのまま**（40pxに詰めると隣と当たり判定が2px重なり、既存の検査が落ちた）。340px以下（初代SE級）だけ見た目を40px・余白8px・数字12pxに詰める。
  - 検査3項目追加：320／360／375／390px幅で「1時間25分・12.8km」を入れても「…」が棚と画面の中に収まる。実機で「…」が押せるかは要確認。
- **v123** **「配る」を1枚に（ロードマップ 段階1-1／UI点検 09）**。編集画面の上バーに「配る」ボタン（PC＝保存の右、スマホ＝保存の左の白い小さなボタン）。押すと `#shareSheet` が1枚出て、**画像で保存／配布シート／リンクを作る／GPXで書き出す** の4つと、**「この地図に載る情報」**（コースの説明の有無・写真つきスポット数・スタンプラリーの対象数）が同じ画面に並ぶ。入口1回＋出口1回の**2タップで4つの出口すべてに届く**（目標は3タップ以内）。
  - 4つの出口は `shareExit(kind)` が既存の `saveMapAsImage()`／`openPrintSheet()`／`openShareDialog()`／`exportGpx()` を**呼び分けるだけ**。出口の中身はいっさい変えていない。リンクは保存済みコースから作るので、未保存なら先に `saveCourse()` してから同じ画面を開く。
  - 説明が空なら「未記入 ›」を色つきで出し、押すとその場で書ける（スマホは v118 の `openDescSheet()`）。写真が1枚も無いときも数を色つきにする。説明が空のまま配る事故を減らすため。
  - スマホは下から出るシート、PC（769px以上）は中央の1枚（`@media` で同じ要素の置き方だけ変える）。歩く人の画面（`body.viewonly`）と埋め込みには入口を出さない。
  - 既存のメニュー（…）の「画像保存・配布シート・GPX」の行はまだ残してある。段階1-2 で「配る」へ寄せる。
  - 検査8項目追加（静的6・機能2）。実データ（波賀町飯見・道なり2.4km）で撮って確認：写真つき 7/14・対象 14か所・説明は未記入。**実機で確かめていないこと**：白い「配る」ボタンが航空写真の上で見えるか、シートの高さがホームバーのある iPhone で収まるか。
- **v122** **読み上げ名と、拡大禁止の解除（ロードマップ 段階0-4／UI点検 06・08）**。文字の無いボタンに読み上げ用の名前（`aria-label`）が無いものが **15個**あった（PCツールバー7・ヘッダ3・写真2・一覧カード3。評価書では7個と書いたが、アプリ全体を数え直すと15個）。すべてに `title` の1行目と同じ言葉を付けた。一覧カードの3つは JS の雛形にあるので、そこにも付けた。閲覧モードのボタンは状態で名前が変わる（閲覧モード／編集にもどる）。**「文字の無いボタンに読み上げ名が無い」＝検査で落ちる**ようにした（HTMLとJSの雛形を静的に数え、画面に実際にあるボタンも数える）。
  - `user-scalable=no` を外した。Android で文字を拡大して読めなかったため（iPhone の Safari はもともと無視する）。**地図の上だけは `#map{touch-action:none}` でブラウザの拡大を止め、ピンチを Leaflet に渡す**＝地図の拡大縮小が二重にならない。
  - **実機で確かめていないこと**：Android で、地図の上のピンチが二重にならないか／メニューや編集画面の上で拡大できるか。ここは手元では確かめられない。
- **v121** **消える前に知らせる（ロードマップ 段階0-3）**。作ったコースは端末のブラウザにしか無く、機種変更・履歴削除・Safari の長期未使用で消える。バックアップは人が思い出して押す方式だった。**一覧画面に「最後のバックアップ：n 日前。保存 m 回ぶんが未反映」を出し続け、30日で黄・60日で赤**にし、その場で「いま書き出す」を押せるようにした（`#backupInfo`／`backupStatus()`）。記録は `LS.backupAt`（「すべて書き出す」をした日時）・`LS.saveCount`（その後の保存回数）・`LS.firstSaveAt`（一度も無いときの起点）。保存が5回たまるたびに保存直後にも一言出す。**保存したことが無いうちは何も出さない**（見本コースだけの人に催促しない）。
  - **iPhone の Safari には「ホーム画面に追加すると消えにくくなります」を1回だけ案内**（`#homeTip`、`LS.homeTipSeen`）。Safari が7日以上使わないサイトの保存を消す仕様に対する、いまできる対策。判定は `_isIosBrowserTab()` に切り出し、検査で差し替えられるようにした。
  - 正直な限界：「書き出した」は端末へのダウンロードを呼んだ時点で記録する。利用者がその場でダウンロードを取り消しても記録は残る（ブラウザからは分からない）。
  - ついでに `#storageInfo` の文字（10.5px・#9A8F80）が v102 の見直しから漏れていたので 11px・`--t-sub` に直した。検査5項目追加。
- **v120** **閲覧中は編集の操作を受け付けない（ロードマップ 段階0-2／UI点検 18）**。閲覧モード（作る人の確認用）と配布リンク（歩く人）で、**編集の操作がそのまま押せていた**。とくに以下は実際に起きる不具合だった：①地図をタップすると種別の選択が開いてスポットを置けた（`onMapClick` に閲覧中の門が無かった）②スポットと調整点の印を指で動かせた ③一覧の並べ替えと ✏ が効いた。`body.viewing` で編集の操作（モード切替・保存・取消・やり直し・全消去・調整点・手動・並べ替え・説明の編集）を隠し、`onMapClick`／`openModal`／`undoLast`／`redoAction`／`clearAll`／一覧のドラッグに `viewMode` の門を付け、印は `dragging.disable()` で動かせなくした（`_applyViewLock`）。作る人の閲覧モードでは **「✎ 編集にもどる」** だけを出す。
  - **配布リンクを開いた人への案内**（`#walkTip`、3行）を最初の1回だけ出す（`LS.walkTipSeen`）。埋め込み表示では出さない。
  - 検査6項目追加（押せる編集操作が0、地図タップ・編集画面・印の移動が効かない、案内が1回だけ）。実データ（配布リンクをスマホ幅で開く）でも確認。
- **v119** **配布リンクに道順を同梱（ロードマップ 段階0-1）**。これまで配布リンクや保存データには**道順の座標が入っておらず、開くたびに経路サーバで計算し直していた**（`segCache` は保存されない）。イベント当日に大勢が一斉に開くと、無償の公開サーバの「軽い利用」を超える恐れがあった。保存データに **`routes`（区間ごとの道順、キーは座標）** を入れ、読み込み時に `segCache` へ戻してから道なり計算に入るようにした。**開くときの経路サーバ呼び出しは0回**（検査と実データで確認）。
  - **直線に逃げた結果は覚えない・保存しない**：サーバが止まったときの直線（fallback）に印を付け、キャッシュにも保存にも入れない。以前は一時的な不通でも直線がセッション中ずっと残っていた（副次的な不具合修正）。
  - 区間キーの式は `buildAnySegment`・`_prefetchOsrm`・`doRouting`（出発点／到着点のつなぎ区間）の**4か所**に直書きされていた。`_segKey()` の1か所にまとめ、「直書きが1つでも残れば落ちる」検査を入れた。
  - **見落としかけた穴**：出発点・到着点が「ルートに含めない」設定のとき、`doRouting` はルートの端とつなぐ2区間も経路サーバに聞く。最初の実装はこの2区間を同梱しておらず、サンプルコース（出発点・到着点がルート上）では0回で通ってしまった。検査を「出発点・到着点をルートの外に置いた4点コース」に変えて捕まえ、つなぎ区間も同梱するようにした。
  - **既存のコースは、一度開いて保存し直すと `routes` が付く**（サンプルコースの `sample.json` には未同梱）。
  - 実測：サンプルコース（125点・2.36km）で同梱は約6KB。検査5項目追加（経路サーバを差し替えた決定的な読み戻し検査＝ネットに依存しない）。
- **v118** **スマホでコースの説明を書けるようにした（UI点検 11・STEP3の1つ目）**。「コース説明・見どころ」はパソコンの左側の欄にしかなく、**スマホではその欄ごと隠れていた**ため、スマホだけで作った人は説明を空欄のまま配ることになっていた。メニューの「コース」に **「コースの説明・見どころ」** を足し、下から出る画面で書けるようにした。中身は**パソコンと同じ `#iDesc` を読み書きする**ので、配布シート・配布リンクの出どころは1つのまま。メニューには **「書かれています／未記入」** を出して書き忘れに気づけるようにした。
  - あわせて、**説明を書き換えても「未保存」にならなかった**のを直した（`#iDesc` の入力を拾う）。これまでは説明だけ書いて一覧に戻ると、警告なしに消えていた。
- **v117** **線そのものを切り分けて三角を挟んだ（オーナー指摘）**。v116 は「1本の破線を引いて、三角の場所だけ白で消す」やり方だったため、**消した所の前後で破線が中途半端な長さに切れて見苦しかった**。やり方を変え、**線を「破線ちょうど n 本ぶん」の切れ端に分けて描く**ようにした（`routeLine` を複数の線の集まりにする）。切れ端ごとに破線が引き直されるので、**破線の長さはどこでもそろう**。切れ端と切れ端のあいだ（三角ぶんのすき間）に三角を置く。
  - 既定は 5 本ぶんの切れ端。スポットの手前では、そこまでの本数（1〜8本）で切れ端を区切るので、**スポット手前の三角も破線の切れ目にきちんと入る**。
  - 白いふち（`routeCasing`）は切らずに1本のままなので、すき間は白く抜けて見える。
  - 白で消す層（`routeDirGaps`, v116）は不要になったので削除。
- **v116** **三角を破線の「間」に入れた（オーナー指摘）**。v115 までは破線の上に三角を重ねているだけで、線の一部が三角の下に隠れていた。**三角を置く場所の赤い破線を白で消してから（`routeDirGaps`）、そこへ三角を置く**ようにした。線の下には白いふち（`routeCasing`）が敷いてあるので、消した部分はそのまま「破線のすき間」に見える。見た目は **— — — ▶ — — —**。
  - 消す線の太さは赤い線＋0.6px、長さは三角＋6px。`lineCap:'butt'` で余計にはみ出さない。
  - `_buildDirMarks()` の戻り値を `{tris, gaps}` に変更（三角の形と、消す線の両方を返す）。
- **v115** **破線を元に戻し、三角の置き方を3つのきまりにした（オーナー指示）**。破線は元の `8,5`（ズームで拡大）に戻し、三角は次のきまりで置く：**①破線5本ごとに1つ（`DIR_EVERY_DASHES`）②スポットの手前に必ず1つ ③スポット手前の三角と、その前の三角の間の破線が3本以下なら、前の「ふつうの三角」を飛ばす（`DIR_MIN_DASHES`）**。三角はどれも破線の切れ目の中央に入る。
  - **スポットが線のどこにあるかは「線に下ろした足」で測る**（いちばん近い頂点ではなく）。往復ずらし表示では線がスポットから少しずれるため、頂点だけを見ると位置がずれる。
  - ○の下に入る三角は置かない（`DIR_AVOID_PX`）。スポット手前の三角は `DIR_AVOID_PX + 三角の大きさ + 4` だけ手前に置くので、○に重ならない。
  - 検査3項目追加（破線5本ごとに並ぶ・スポット手前に出る・○に重ならない）。
- **v114** **破線の切れ目に三角をそろえた（`──── ▶ ──── ▶`）（オーナー指示）**。三角を 5px→**7px**にし、**破線の周期＝三角の間隔**、**破線の切れ目の幅＝三角がちょうど収まる幅**にして、三角が必ず切れ目の中央に来るようにした。線も三角も「線に沿った距離」で位置が決まるため、計算だけでぴったりそろう（位置合わせの特別な仕掛けは要らない）。破線の寸法は `_routeDash()` が `DIR_GAP_PX` と `DIR_TRI_PX` から計算するので、**間隔や大きさを変えると破線も自動で追従する**。
  - 配布シートの凡例も同じ見た目（`──▶`）にして1行にまとめた（以前は「歩くコース」と「曲がり角」の2行で、地図の見た目とも食い違っていた）。
- **v113** **進行方向の三角の寸法調整（オーナー指定）**。間隔 30px→**50px**、大きさ 8px→**5px**、○を避ける距離 17px→**10px**。あわせて**白いふちの太さを三角の大きさに比例**させた（固定 2.2px のままだと、5px の三角がふちで埋まって形が残らないため）。数値以外の作りは v112 と同じ。
- **v112** **進行方向の三角を一定間隔に（オーナー指示）**。v111 の「曲がり角だけ」から、**ルート線の上に 30px ごとに小さな三角**を並べる方式へ変更。三角は塗りつぶし＋白フチで、大きさ 8px（v111 の 11px より小さい）。線は**赤い破線のまま**。
  - 間隔は**画面上の px** なので、拡大しても密度は変わらない（`DIR_GAP_PX`）。
  - **スポットの○に近い位置（17px以内）は飛ばす**。○の下に入ると三角が見えないため（v111 で実際に起きた）。飛ばした分だけ間隔が2倍・3倍になるので、検査も「決めた間隔の整数倍か」で見ている。
  - 定数は `DIR_GAP_PX`（間隔）・`DIR_TRI_PX`（大きさ）・`DIR_AVOID_PX`（○を避ける距離）の3つ。**この3つの数字だけで見た目を調整できる**。
  - 経緯：v108 破線＋まばらな矢印 →（見づらい）→ v109 矢印の連なり →（見づらい）→ v110 取り下げ → v111 曲がり角だけ → **v112 一定間隔**。線に重ねる印は、**塗りつぶした三角**が細い線の「>」より読みやすい（Leaflet の標準プラグインも塗りつぶしの三角を使う）。
- **v111** **曲がり角に小さな三角（進行方向）**。線の全体に矢印を並べる v108/v109 が読みにくかったため、**自転車ルートの世界標準「ダン・ヘンリー方式」**（米国自転車連盟が標準化）にならい、**曲がる所だけに印を出す**方式にした。直線には何も置かない＝「印が無い＝そのまま進む」。ルート線は**赤い破線のまま**（v103の状態）で、その上に**白フチの小さな赤い三角**を重ねる。
  - **角の見つけ方**：隣り合う点の角度では判定できない（OSRMのルートは数mおきに点があり、隣どうしの角度はどこも数度にしかならず、実際に最初の実装では 2 個しか出なかった）。**線に沿って前後 `TURN_LOOK_PX`(22px) 進んだ位置との向きの差**で判定する方式に書き直した。これで点の細かさに左右されない。
  - **置く場所**：角の少し先（`TURN_CLEAR_PX`=18px＋三角の大きさぶん）。**角がスポットと重なるとき、○の下に三角が隠れて見えなかった**ため（実際に画面で確認して判明）。向きも「置いた場所の線の向き」に合わせている（角の途中の向きだと三角が線に対して傾いて見えた）。
  - 画面に入っている所だけ作り、`zoomend`/`moveend` で作り直す。大きさは `TURN_MAX_K` で頭打ち。完全に表示専用（`interactive:false`／当たり判定に使わない／`removeLines()` で消す／データに触れない）。
  - 配布シートの凡例に「曲がり角（三角の向きに進む）」を追加。検査5項目追加（曲がる所にだけ出る・まっすぐな所には出ない・進行方向を向く）。基準画像を作り直し。
- **v110** **進行方向の矢印を取り下げ、v107 の見た目に戻した（オーナー指摘）**。v108（破線＋まばらな矢印）も v109（矢印の連なり）も、実機を待つまでもなく「見づらい」との判断。**線の上に矢印を重ねるやり方そのものが合っていなかった**：破線と並べると競合し、連ねると線が矢印で埋まって道の形が読めなくなる。`index.html`・回帰テスト・基準画像を v107 の内容へ戻し、版数だけ v110 に上げた（`git checkout 3e64dd2 -- …`）。ルート線は**赤い破線＋白いふち**（v103 の状態）に戻っている。進行方向の示し方は、他のアプリの実例を調べ直してから作り直す。
  - **あわせて回帰テストの不安定さを1つ直した**：地名の検索（Nominatim）はネット任せで、応答が遅れると**検査が地図を固定したあとに地図を動かしてしまい**、「検査用コースが地図の中に収まっている」が落ちることがあった（今回1回発生し、再実行で通った＝不安定な検査）。検査中は `geocode` を固定の座標を返す関数に差し替えて、ネットに依存しないようにした（手元で3回連続 PASS）。
- **v109** **矢印の連なりに作り直した（オーナー指摘）**。v108 の見た目は「破線＋90pxごとの小さな矢印」で、実際には **`--->・---->・`** のように見えて読みづらかった。**破線をやめ、矢印を 13px 間隔で連ねて `>>>>>` の見た目**にした。線は矢印どうしをつなぐ細い糸（1.5〜3.2px）に格下げし、主役を矢印にした。
  - **拡大したときの作り方を変えた**：矢印は「画面に入っている所だけ」作る（拡大するとルートの長さが画面上で数万pxになり、全部作ると重くなるため）。そのため `zoomend` に加えて **`moveend` でも作り直す**。上限 `ARROW_MAX=900` は保険。
  - **大きさに上限**（`ARROW_MAX_K=1.6`）を付けた。上限が無いと拡大時に矢印が巨大になり、かえって読みにくかった（実測して調整）。
  - **配布シートの凡例も地図と同じ見た目**（赤い線＋白フチの矢印）にし、文言を「歩くコース（矢印の向きに歩く）」に変えた。地図と凡例が食い違わないようにするため。
  - `_routeDash()` は使われなくなったので削除。検査3項目追加（破線を使っていない・大きさに上限がある・矢印が6個以上連なる）。基準画像を作り直し。
- **v108** **進行方向の矢印（オーナー要望）**。配った地図で「どちら向きに歩くのか」が分かるよう、ルート線の上に矢印（>）を並べた。**画面上90px間隔**で置くので、拡大しても矢印が密になりすぎない。矢印の大きさは線の太さと同じ倍率で変わる。**往復ずらし表示の座標を使うため、行きと帰りにそれぞれ向きの違う矢印が出る**（往復コースでどちら回りかが一目で分かる）。線と同じく**白いふちを敷いてある**ので航空写真の上でも読める。`routeCasing` と同じく**完全に表示専用**：`interactive:false`・当たり判定に使わない・`removeLines()` で消す・データには触れない。保存画像と配布シートにも出る。**常に表示**（切り替えは今は無し。不要なら1行で消せる）。検査4項目追加（西→東の線なら右向き、東→西なら左向きに矢印が向くことを実測）。基準画像を作り直し。
- **v107** **最初の1回だけ出す使い方案内（UI点検 14）**。新しいコースを作った直後の画面は**地図と小さなアイコンだけの白紙**で、「地図をタップするとスポットが置ける」と分かる手がかりが1つも無かった（特にスマホはツールバーごと隠れているため案内文も出ない）。スポットが**0個のときだけ**、地図の中央に「地図をタップして最初の場所を置いてみましょう」を出す。**「はじめる」を押すか、最初のスポットを置いた時点で以後は二度と出さない**（`LS.tipSeen`）。配布リンク・埋め込み表示では出さない。`#map` の外（`#mapArea` 直下）に置いてあるので、**保存画像や配布シートには写らない**。検査5項目追加。
- **v106** **指で押す所を 44px に（UI点検 06）**。歩きながら・屋外で押せるよう、押す所の最小を `--tap:44px` と決めて1か所に集約した（Apple/Google の目安）。見た目を大きくしすぎないよう、**見た目は 40px のまま「押せる範囲」だけを 44px に広げる仕掛け**（`.tap` と `::after`）を用意し、並べて置くボタンは「見た目＋すきま」が 44px 以上になるよう間隔を調整した（重なると隣を押してしまうため）。モード切替だけは実寸で 44px にした。一覧カードのボタン（書き出し・リンク・削除）も 30px→40px。
  - **この作業で見つかった不具合2つ**（どちらも実際に当たり判定を調べて判明）：①**閲覧モード中、スタンプ表示が「…メニュー」ボタンを完全に覆っていた**（`#stampBar` の z-index 1200 がシェルフ 1000 の上に乗っていた）。スマホでは左上（戻るボタンの下）へ移した。②右の浮きボタン（取り消し）が「保存」の押す範囲に重なっていたので 8px 下げた。
  - 検査は**画面上の点を実際に押せるか**（`elementFromPoint` で44px四方の4点を調べる）方式にした。大きさを測るだけでは、上に別のものが乗っている①のような不具合は見つけられない。
- **v105** **ツールバーの分かりにくさ（UI点検 01・14の一部）**。PCのツールバーに **「調整点」という同じ文字のボタンが2つ並んでいた**（片方は点を置くモード、片方は表示の切り替え。違いはマウスを乗せた説明文にしかなかった）。切り替えのほうを **「調整点の表示」** に改めた。あわせてツールバー下の案内文が**右端で見切れていた**（実測：中身309px／表示幅191px）ので、短い文にして**全文はマウスを乗せると出る**ようにした。画面を開いた直後の文言は HTML に直接書いてあり `setMode` の文言と食い違っていたので、こちらもそろえた。検査4項目追加（同じ文字のボタンが並ばないこと・案内が見切れないことを実測）。
- **v104** **ラベル位置に「自動」を足した（UI点検 02）**。v99 で自動配置を入れたとき、**`labelDir:'top'` を「自動でよい」の合図として流用した**ため、編集画面で「上」を選んでいるのに勝手に下や左へ動く、という**表示と動きの食い違い**が生じていた。上に固定する方法も無かった。選択肢の先頭に **「自動（おすすめ）」** を足してこれを既定にし、`'top'` は文字どおり上に固定する意味に戻した。**古いコースの `'top'` は読み込み時に `'auto'` へ読み替える**ので、これまでのコースの見た目は変わらない（実測：保存済みコースを読み込むと `labelDir` はすべて `auto` になり、自動配置が働いて bottom/right へ逃げる）。検査4項目追加。
- **v103** **地図の上の見え方（デザイン方針 STEP1の仕上げ）**。**ルート線の下に白い実線を敷いた**（`routeCasing`／`ROUTE_CASING_ADD`）。航空写真・地形図・淡色地図の上では赤い破線が背景に沈んで見えなくなっていたため。**白いふちは完全に表示専用**で、赤い線と同じ表示座標（`_buildDisplayCoords` の結果）を共有し、往復ずらし表示ともズレない。当たり判定（`hitOverlays`）は付けず、`_routeLineBase`・`_lastRouteCoords`・高低差・保存データには一切触れない。あわせてスポットの○の白いふちを `2px rgba(255,255,255,.85)` → `2.5px #fff` にして輪郭をはっきりさせた。検査を5項目追加（白いふちが赤い線と同じ形か・当たり判定に使っていないか・後片付けするか・データが変わっていないか）。基準画像を作り直し。
  - **注意**：この版の見た目の変化は、**基準画像との比較検査では検出できなかった**（線の太さの差が画素全体の1%に届かないため）。線の描き方を変えたときは、比較検査の合格を根拠にせず、必ず基準画像を作り直して目視で確かめること。
  - **検査の直し（アプリは変えていない）**：v101〜v103 を公開したあと、GitHub 上の自動テストだけが「経路サーバへ同時に投げすぎない」で落ちた（peak 6 / 上限 4）。原因は**アプリではなく検査の作り**で、`window.fetch` を丸ごと差し替えて**すべての通信**を数えていたため、標高や `version.json` の通信がたまたま重なると経路の同時数を測ったことにならなかった。`/route/v1/` を含むものだけ数えるようにし、それ以外は素通しするよう直した（手元で4回連続 PASS）。
- **v102** **文字の大きさと濃さ（デザイン方針 STEP1の続き）**。読ませる文の**最小を10px→11px**にし、薄すぎた文字色を `--t-sub:#6B6055` の1つにまとめた（アプリのどの下地の上でも 5.0:1 以上）。直した実測値の例：「地図をクリックして追加」2.16:1、一覧の住所 2.52:1、ツールバー下の案内 3.04:1、スマホメニューの見出し・単位・シェブロン（#B0A99F/#A8A29E/#C4BEB8）はいずれも 2〜3:1 台だった。**v101 で見落としていた3つ目の操作色 `#F97316`**（スマホの保存ボタンとモード選択中の文字）も見つけて `--brand` に統一した。高低差グラフの目盛り文字（SVG）は大きさを変えず色だけ濃くした（小さな折れ線の中の数値のため。`COLOR.sub`）。検査を4項目追加し、**ブラウザ上で実際に描かれた文字の大きさと濃さを毎回測る**ようにした（静的な文字列検査だけでは、この種の見落としを防げなかったため）。配布シートの高さが2px伸びたので基準画像を作り直し。
- **v101** **配色の土台（デザイン方針 STEP1）**。**操作の色が2種類（#F2670E と #C4703A）混在していた**のを1つにまとめ、`:root` の CSS 変数 `--brand` ほかに集約した。理由は見た目の好みではなく実測：**白い文字を載せたときの読みやすさが #F2670E は 3.12:1、#C4703A は 3.67:1、グラデーションの明るい端 #FB8B3E は 2.37:1** と、いずれも基準（4.5:1）に届いていなかった。新しい `--brand:#B8440A` は **5.43:1**、グラデーションの明るい端 `--brand-lite:#C25313` は **4.62:1**。あわせてスポットの色を4つ入れ替え：**1型色覚では「スタート緑 #2E7D32」と「コースポイント橙 #C4703A」がほぼ同じ色に見えていた**（12色66通りを Viénot ほか(1999) の式で1型・2型に変換して測定：離れ具合 **3.1**）。スタートを #0E6B3C、コースポイントを #A8551F にして **7.8** まで離した。公園 #00897B→#00796B・その他 #757575→#5F5F5F は白文字の基準を満たすため。**色だけでは限界があるので、S・G・◎ などの記号との併用が前提**（次版以降で強化）。色は「CSSの `:root`」と「CONSTANTS の `COLOR`」の2か所にあり、**食い違わないことを回帰テストで検査**している（SVGとLeafletはCSS変数を使えないため）。テスト+5項目、見た目の基準画像を作り直し。
- **v100** **スタンプラリー**（提案：Stroly由来）。配布リンクで歩く人が、スポットに50m以内まで近づくと記録が付く。**閲覧モードのときだけ**働く（作る人の画面には出さない）。記録は `LS.visits` に**コースIDごと・その端末だけ**に保存し、**コースの中身は変えず、どこにも送らない**。訪問済みの○には右上にチェック、画面右下に「スタンプ n / m」（押すと全消去）。現在地追従（v97）と組み合わせると、歩きながら自動で貯まる。
- **v99** **地名ラベルの重なり自動回避**（提案11）。密集したラベルを空いている向き（上→右→下→左）へ自動で逃がす。**利用者が「上」以外を選んだラベルは動かさない**。自動の向きは `_autoDir` に持ち、**保存データ(`labelDir`)は変えない**。スポットの○も障害物として扱い、ラベルで他のスポットを隠さない。実測：サンプルコース（ラベル11枚）で重なり **2組→0組**（4枚を移動）。
- **v98** **スポットの色の整理**（デザイン改善）。**ビュースポットと駐車場が同じ色(#1565C0)**だったのを含め、紛らわしい4色を変更（飲食店→濃ピンク／史跡→紫／公園→ティール／駐車場→藍）。印刷・画像でも見分けられる。あわせて**色の定義を `WT` の1か所に集約**：`.wp-tt-*` のCSSは起動時に `_injectWpStyles()` がWTから生成し、画像保存の色マップもWTから作る（以前はCSS・WT・画像保存の3か所に同じ色を直書きしており、直し漏れの温床だった）。
- **v97** **現在地追従**（提案6）。スマホメニューに「現在地を追う」スイッチ。`watchPosition` で現在地の印と精度の円を出し、**4m以上動いたときだけ**地図を寄せる（小刻みな揺れで暴れない）。**最初の1回だけ倍率を合わせ、以後は寄せるだけ**（毎回 setView すると利用者が縮小しても引き戻してしまうため）。コース範囲外では地図を動かさない。一覧に戻る／ページを離れるときは必ず止める（電池対策）。
- **v96** **GPX読み込み**（提案：GPXの読み込み）。起動画面の取り込み口が **JSON / GPX 両対応**に。`<wpt>` はスポットとして取り込み、自分で書き出したGPXなら `<type>` から種別も復元する。**`<wpt>` があるときは軌跡を取り込まない**（実測でルートが二重になり 2.36km→7.64km に狂ったため）。`<wpt>` が無い「歩いた記録」だけのGPXは、始点/終点をスタート・ゴールにし、軌跡を細道として取り込む＝その道に沿う。点は5m間隔に間引き・最大2000点。**GPXには調整点(VP)の仕組みが無いので、書き出して読み戻すと道順は引き直しになる**（実測 2.36km→7.43km）。取り込み時にその旨を明示し、道順を残したいときはJSONを使うよう案内する。
- **v95** **コースの難易度**（提案2）。距離と登りの合計から「やさしい／ふつう／健脚」を自動判定し、サイドバーと**配布シート**に表示（`DIFFICULTY`：3km・登り100mまで＝やさしい／6km・登り300mまで＝ふつう／それ以上＝健脚）。**高低差が取れていないときは表示しない**（分からないものを断定しない）。実測：サンプルコース（2.36km・登り141m）は「ふつう」。あわせて見た目の比較検査でも難易度を検査対象にした（高低差はルート再計算で消えるため、計算が落ち着いてから固定値を入れる）。
- **v94** **ホームページへの埋め込み**（提案14）。配布リンクに `&embed=1` を付けると、ヘッダ・サイドバー・編集UIをすべて隠して**地図だけ**を見せる表示になる（`body.embed`）。上部の帯に コース名・総距離・「大きな地図で開く」（`embed` を外したURL）を出す。配布リンクのダイアログに**貼り付け用の `<iframe>` コード**とコピーボタンを追加。市のホームページに1行貼るだけでコース地図を置ける。
- **v93** **経路サーバの待避**（提案9＝残っていた最大の弱点）。`ROUTERS` に同じ形式(OSRM API)のサーバを並べ、1台目（公式デモ）が止まっていれば **FOSSGIS の予備サーバへ自動で切り替える**（切替は利用者に通知）。**同時アクセスを4件までに制限**（`ROUTER_MAX_PARALLEL`）。v93以前は**20区間を一斉に投げており**、提供元の「1秒1回まで」に反するうえ、止まったサーバを毎区間試して待たされる原因になっていた（実機で確認：1台目への試行が20回→**4回**に減少）。だめだったサーバは `ROUTER_COOLDOWN_MS` の間休ませ、全滅時も同様に休止して操作が止まらないようにする。動作確認画面は**全サーバの応答を個別に表示**する。※サーバによってルート形状・距離は数%変わる（データ更新時期の違い）。
- **開発の土台（v92と同時）** ①`ARCHITECTURE.md` を復元（設計・不変条件・非同期の約束・セクション地図）。②`.github/workflows/test.yml` を追加し、**push のたびに GitHub 上で回帰テストが自動実行**される。③**見た目の比較検査**を追加（`tests/baseline/` の基準画像と画素比較）。固定コースを直線・タイル無し・基準点固定で描き、保存画像と配布シートを比べる。文字の描画がOSで違うため、基準を作った環境でのみ比較する（`PLATFORM.txt`）。④出荷手順スキル `.claude/skills/release/SKILL.md`。
- **v92** **新しい版を自動でお知らせ**（`?v=` の手作業を廃止）。公開所の `version.json` と実行中の `APP_VERSION` を比べ、違えば一覧画面の上に「新しい版 vNN が公開されています／更新する」を出す。押すと `?v=NN` 付きで開き直し、サービスワーカーも更新する。**案内は一覧画面のときだけ**出す（地図画面ではスマホの戻る/保存に重なり、編集中の更新は未保存を失うため）。**出荷時は index.html / version.json / package.json の3か所の版数をそろえること**（回帰テストが一致を検査）。あわせて v91 の動作確認画面を**普段の画面から外し**、`?check=1` を付けたときだけ開く不具合調査用にした。
- **v91** **動作確認（不具合調査用・`?check=1`）画面**。開発者ツール無しで、実機で「本当に動いているか」を調べられる。版・ネット接続・部品(Leaflet/html2canvas/QR)の読込・保存領域・写真の保存先・サービスワーカーの状態・保存タイル数・画面/タッチ/GPS権限、さらに**地図タイル／経路サーバ／標高APIへ実際に1回ずつ接続して応答時間まで測る**。入口は 起動画面の「動作確認（実機チェック）」リンク・スマホメニュー・`?check=1`。「結果をコピー」でテキスト化でき、そのまま開発側へ送れる。圏外確認の手順も画面内に記載。
- **v90** **オフライン対応**（提案5）。`sw.js`（サービスワーカー）を追加。**アプリ本体はネット優先**（つながる時は必ず最新を取りに行くので「古い版のまま固まる」事故が起きない）、**地図タイルはキャッシュ優先**（上限1200枚・超過分は古い順に削除）、**CDNライブラリはキャッシュ優先**（URLに版が入っており不変）。インストール時に本体を先に確保するので、初回表示の直後から圏外に耐える。スマホメニューに**「地図を持ち歩く」**（コース範囲のタイルを現在の縮尺から2段階ぶん、最大600枚まで先読み。6件ずつ取得）。圏外時はバッジ表示。**困ったときは URL に `?nosw=1` を付けて開くと、この仕組みを丸ごと解除できる**。検査は file:// では登録できないため、回帰テストがローカルHTTPで配信し `set_offline(True)` で実際に圏外にして確認する。
- **v89** **写真の保存先を IndexedDB へ**（提案8＝残っていた唯一の「高」）。コース本体（localStorage 約5MB）には参照 `idb:<id>` だけを持たせ、写真の実体は IndexedDB に置く。**書き出し（JSON・配布リンク・バックアップ）は実体を埋め込み直す**ので「1ファイルで完結」は従来どおり。起動時に `migratePhotosToIdb()` が既存コースを自動移行し、`gcPhotos()` が参照されていない写真を片づける。IndexedDB が使えない環境では実体のまま保存＝従来動作（`_idb()` が null を返すだけ）。表示は `_photoAttr()`＋`_fillPhotoImgs()` で参照を解決。**`saveCourse()`・`exportCourse()`・`applyBackupData()` 等は非同期になった（呼び出し側は await 必須）**。あわせて一覧画面に**保存領域の使用量**を表示（3.5MB超で警告）。実測：サンプルコース（写真7枚）で localStorage が **約1.7MB → 0.009MB**。
- **v88** 使いやすさの小改善。(1) スマホメニューの「文字の大きさ」は**項目自体をその大きさで表示**、「○の大きさ」は**実比率の丸**を並べる（選ぶ前に違いが分かる）。見本の値は `LABEL_SIZES`/`WP_SIZES` から `_syncSizeMenu()` が設定するため、定数が唯一の出どころ。(2) コースが0件のときの画面に「＋ 新しいコースを作成から始められます」の案内を追加。
- **v87** 配布シートに**QRコード**。コースごとの配布リンク（v82）を `LS.shareLinks` に記憶してシートに載せる。紙の地図から、そのままスマホの地図へ誘導できる。リンクを作っていないコースではQR欄を出さない。QRライブラリ（cdnjs の qrcodejs 1.0.0）が読み込めない環境ではQRを省くだけでシートは通常どおり使える。**CDN参照は3→4に増加**（回帰テストの本数を更新し、4つとも版が固定されていることも検査）。
- **v86** 使いやすさ2件。(1) **未保存のまま閉じる前に確認**（提案12）：`_dirty` は編集の共通入口 `saveSnapshot()` で立て、保存・読込・一覧復帰で下ろす。閲覧リンクでは出さない。(2) **歩く速さの選択＋坂を考慮した所要時間**（提案7）：`WALK_SPEEDS=[3,3.5,4,4.5,5]`、既定4km/h（従来と同じ）。高低差を取得済みなら登り100mにつき+10分（`CLIMB_MIN_PER_100M`／登り600mで1時間＝ネイスミスの法則の一般的な係数）を加算し、表示に「登り込み」と明記。設定は保存（`LS.walkSpeed`）。PCはサイドバーの選択欄、スマホはメニュー。**注意：高低差を取得しているコースは所要時間の表示が従来より長くなる**（例：サンプルコースは約35分→約49分）。より実態に近い値。
- **v85** 配布シート（提案4・10）。地図画像＋タイトル・総距離・所要時間・**凡例・縮尺バー・方位・見どころ・スポット一覧・出典**を1枚にまとめ、`@media print`＋`@page{size:A4 landscape}` で **A4横に印刷／PDF保存**できる。「画像で保存」で同じ1枚をPNGにもできる。縮尺は画面中央の緯度で 1px→m を実測し、キリのよい距離を選んで**画像幅に対する割合**でバーを描く（印刷で縮んでも比率が保たれる）。凡例はそのコースで実際に使っている種別だけを地図と同じ○で表示。あわせて地図の画像化を **`_captureMapCanvas()`** に切り出し、「画像保存」と共用（html2canvas 対策を1か所に集約）。
- **v84** **画像保存の後片付けが実行されていなかった不具合を修正**。`saveMapAsImage` は `try` ブロック内で `const _savedVpOpacity` 等を宣言し、`finally` からそれを参照していた（ブロックスコープ外＝`typeof` 判定が常に偽／`_savedVpOpacity[i]` は ReferenceError）。このため **画像保存すると調整点(VP)が opacity:0 のまま消え**、ツールチップの撮影用スタイル（box-shadow除去等）も戻らなかった。実機ブラウザで再現確認済み（VP17個が非表示のまま＋ReferenceError）。修正は「保存値の巻き戻し」ではなく **`updateTooltip()`/`refreshIcons()` で正しい状態を作り直す**方式（`!important` の欠落も避けられる）。`saveMapNoText` は宣言が try の外にあり同種の問題なし。
- **v83** GPX書き出し（提案2）＋全コース一括バックアップ／復元（提案3）。GPXは 1.1 準拠（metadata→wpt→trk の順、wpt内は name→desc→type）。`<trk>` には**表示用ではなく実データ** `_lastRouteCoords` を使う（往復ずらしは表示専用のため）。なぞり端点(node)は除外。PCヘッダーとスマホメニューの両方にボタン。バックアップは `{app,type:"backup",version,savedAt,count,courses[]}`。復元は同IDを上書き・無いものを追加（二重に増えない）。容量超過時は警告。
- **v82** 配布用リンク（提案1）。`?course=ファイル名.json` で**誰の端末でも開ける閲覧ページ**になる（JSONを index.html と同じ場所に置くだけ）。コース一覧の各カードに「配布用リンクを作る」ボタン（ファイル名決定→その名前で書き出し→URLコピー→この端末で確認）。安全のため読めるのは同じ場所の `.json` のみ（`_safeCourseFile` が `../`・外部URL・`/`・`:` を拒否）。見る人の端末には保存せず、サンプルも取り込まない。閲覧時は `body.viewonly` で編集UI（ツールバー・保存ボタン・並替ヒント・モバイルの編集シェルフ）を隠し、コース説明があれば読み取り専用で見せる。
- **v81** 不具合修正3件＋共有情報。(1) **保存の失敗を検知して警告**：`setCourses` が成否を返すようにし、容量超過時は「保存できませんでした」と明示（従来は失敗しても「保存しました」と表示され、消えていた）。「保存して戻る」も失敗時は戻らない。JSON読み込み時も同様に確認。(2) **経路サーバに繋がらないとき通知**（従来は黙って直線になり原因が分からなかった）。(3) **標高取得を6件ずつに分割**（50件同時 → ブラウザの同時接続待ちで8秒タイムアウトに達し「取得失敗」になる事象を防ぐ。提供元サーバへの配慮も兼ねる）。(4) `manifest.json`・`theme-color`・OGP を追加（Androidでもホーム画面追加が正しく動き、URL共有時にカード表示される）。
- **v80** スマホの…メニューに「文字の大きさ（地名ラベル）5段階」「スポット○の大きさ3段階」を追加（未対応E）。あわせて設定を localStorage に保存し、再読込しても保持（従来はセッション内のみ）。○の大きさは `_wpSize()` 1か所に集約し、○内文字（`_wpFont`）とラベル位置（○の半径+4px）が自動追従。**標準のままなら見た目は v79 と完全に同値**（スマホ○36px/ラベル位置22px、PC○30px/19px）。表示のみの変更で、座標・ルート・高低差のデータには触れない。

---

## 10. 未対応・計画中の作業
オーナーと方針合意済み/検討中のもの（着手前に必ずオーナー確認）。
- **D. iPhoneの○をズーム連動で拡縮**：現状 WP円は固定px（`isMobile()?[36,36]:[30,30]`、Leaflet標準で退行ではない）。zoomend で円サイズ・文字・ラベル位置を再計算し、**クランプ付き**で「ある程度」拡縮する新機能。中リスク（全マーカー再描画・オフセット再計算と併存）。
- ~~**E. iPhone にサイズ設定**~~ → **v80 で実施済み**（スマホメニューに文字5段階＋○3段階、設定は保存）。残: PCツールバーには○の大きさボタンが無い（必要なら「文字」ボタンの隣に追加できる）。
- **G. PC高低差パネルの移動・リサイズ**：`#elevDetailPanel` は `position:fixed` の DOM。ヘッダードラッグ＋リサイズハンドルで実現可能。高さ可変はチャート再描画対応が必要。
- **H. 地図の色**：要素別（森だけ/道路だけ）の色変更はラスタタイルでは不可。**(b)=別タイル追加は v78 で実施済み**。残る選択肢は (a) タイル全体に CSS フィルタで一括色味替え、(c) ベクトルタイル＋独自スタイル（大改修・ライブラリ＋APIキー要）。
- **その他**：地名ラベルサイズの恒久化は **v80 で対応済み**（`LS.labelSize`/`LS.wpSize`・`restoreSizePrefs()`）。

### 10-2. コード全体レビューで洗い出した改善案（v81時点・優先度順）
着手前にオーナー確認。番号は改善提案ページと対応。

**残っている問題（未修正）**
- ~~[高] 閲覧用リンクが同じ端末でしか開けない~~ → **v82 で解決**（`?course=ファイル名.json`）。`?view=<id>` は「この端末で確認」用として残置。
- ~~[高] 写真が localStorage を圧迫~~ → **v89 で解決**（写真は IndexedDB。本体は参照のみ）。
- ~~[中] 経路サーバが OSRM 公開デモ~~ → **v93 で待避策を実装**（予備サーバへ自動切替・同時数制限）。ただし2台とも公開の善意サーバである点は変わらない。
- ~~[中] タブを閉じる/再読込で未保存の変更が消える~~ → **v86 で対応済み**（未保存時のみ確認）。
- ~~[中] オフライン不可~~ → **v90 で対応済み**（一度見た範囲＋「地図を持ち歩く」で保存した範囲は圏外でも表示）。
- ~~[低] 所要時間が歩行4km/h固定・坂を考慮しない~~ → **v86 で対応済み**（速さ5段階＋登り加算）。
- **[低・v135で方針確定] `user-scalable=no`** でブラウザ拡大ができない（見えづらい人に不利）。

**提案（効果／手間）**
1. ~~誰でも開ける公開リンク~~ → **v82 で実施済み**。~~QRコード化~~ → **v87 で実施済み**
2. ~~GPX 書き出し・読み込み~~ → **v83（書き出し）・v96（読み込み）で実施済み**
3. ~~全コース一括バックアップ／復元~~ → **v83 で実施済み**
4. ~~印刷・配布用レイアウト~~ → **v85 で実施済み**
5. ~~オフライン対応~~ → **v90 で実施済み**
6. 現在地追従モード（歩きながら使う）… 中／中
7. ~~歩行速度の選択＋坂を考慮した所要時間~~ → **v86 で実施済み**
8. ~~写真を IndexedDB へ退避~~ → **v89 で実施済み**
9. ~~経路サーバのフォールバック~~ → **v93 で実施済み**
10. ~~保存画像に凡例・縮尺・方位・タイトル帯~~ → **v85 の配布シートで実施済み**（通常の「画像保存」は地図のみのまま）
11. 地名ラベルの重なり自動回避 … 中／大
12. ~~閉じる前の確認（未保存時のみ）~~ → **v86 で実施済み**

**デザイン改善**
- PCツールバーを「作る／見せ方／出す」の3組に区切る（12ボタンが等価に並び迷いやすい）
- WP種別12色は記号優先＋色は4〜5系統に整理（印刷・画像で識別しづらい）
- ~~文字サイズ設定は項目名自体をその大きさで表示~~ → **v88 で実施済み**
- ~~コース0件時の画面を案内に~~ → **v88 で実施済み**
- 高齢利用者配慮：拡大許可・タップ領域・薄墨文字のコントラスト
○内文字の縦中心は html2canvas で約+1%（ほぼ中央、必要なら微調整）。

---

## 11. ファイル一覧（`/mnt/user-data/outputs/`）
- `index.html` … 現行 = **v79**（配信物）
- `sample.json` … サンプルコース（**index.html と同じ場所に置く**。1.7MB、写真7枚込み）
- `footpath_regression.py` … 回帰テスト（54項目・実コース埋め込み）
- `ARCHITECTURE.md` … 設計・不変条件・描画パイプライン
- `footpath_regression_README.md` … 回帰テストの内訳
- `HANDOFF_開発引き継ぎ書.md` … 本書
- `フットパスマップメーカー_使い方マニュアル.html` / `.docx` … エンドユーザー向け使い方
- `apple-touch-icon.png` / `icon-192.png` / `icon-512.png` / `icon-1024.png` … PWA アイコン
- `index_vNN_backup.html`（v62〜v78）/ `index_ROLLBACK_v4.12.html` … 各版バックアップ
- `preview_label_circle.png` 他 … 検証用スクリーンショット

---

## 12. 既知の制約・注意点
- **単一ファイル・グローバル状態**：状態は全てグローバル。新状態は STATE 区画にまとめ用途コメントを付ける。
- **マーカーは画面固定サイズ**（Leaflet標準）。ズーム連動は未実装（D項）。
- **背景地図3〜5は外部サーバー依存**：日本からの速度・稼働は環境次第。OpenTopoMap 等は低負荷利用が前提で、attribution を各レイヤに設定済み。読めない地図は該当1種だけ外せる。
- **保存画像の最終見え・実機タッチ感はサンドボックスで完全再現不可** → 実機/公開URLで確認。
- **localStorage 依存**：ブラウザデータ消去でコースも消える。JSON 書き出しがバックアップ手段（ユーザーにも案内済み）。**保存容量は概ね5MB**。写真は1枚あたり最大800px/JPEG品質0.65（実測 約0.1〜0.2MB）なので、写真を多く付けたコースが数本たまると上限に達する。v81 以降は超過時に警告が出る（黙って消えない）。
- **写真は書き出しJSONには data URI で埋め込まれる**（1ファイルで完結させるため。sample は約1.6MBが写真）。v89 以降、**ブラウザ内の保存では IndexedDB に分離**されるので localStorage は圧迫しない。

---

*本書は v81 時点の内容です。以降の変更は本書と `ARCHITECTURE.md`・`APP_VERSION` を更新してください。*
