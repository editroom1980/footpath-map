# フットパスマップメーカー — コード開発（Claude Code）への移行手順

チャットでのやり取りから、**Claude Code**（コードを直接編集・テスト・コミットできる AI ツール）での開発に移すための手順です。
非プログラマの方でも進められるよう、画面操作中心（ターミナル不要）の方法を主に案内します。

---

## 0. 用意したもの（このフォルダに入っています）
コード開発に必要なものは、こちらで揃えました。

| ファイル | 役割 |
|---|---|
| **CLAUDE.md** | Claude Code が**自動で読み込む作業規約**（守るべきルール・コマンド・注意点） |
| **package.json** | テスト用の依存と、`npm test` / `npm run serve` コマンド |
| **.gitignore** | Git 管理から外すもの（node_modules・一時ファイル・旧バックアップ） |
| **footpath_regression.py** | 回帰テスト（**どの環境でも動くよう修正済み**・全54項目） |
| **ARCHITECTURE.md** | 設計・描画パイプライン・不変条件 |
| **HANDOFF_開発引き継ぎ書.md** | 全体像・変更履歴・未対応作業・検証環境 |
| **footpath_regression_README.md** | 回帰テストの内訳 |
| index.html / sample.json / アイコン類 | 配信物（アプリ本体・サンプル・PWA アイコン） |

---

## 1. リポジトリに置くファイル
GitHub リポジトリ `editroom1980/footpath-map` の**ルート（同じ場所）**に、次を置きます。

- `index.html`（v79）
- `sample.json`
- `icon-192.png` / `icon-512.png` / `icon-1024.png` / `apple-touch-icon.png`
- `CLAUDE.md`
- `package.json`
- `.gitignore`
- `footpath_regression.py`
- `ARCHITECTURE.md` / `HANDOFF_開発引き継ぎ書.md` / `footpath_regression_README.md` / 本手順書

> `node_modules/` や一時ファイル、`index_v*_backup.html` は `.gitignore` で自動除外します。
> **バックアップHTMLはもう不要**です（元に戻す機能は Git が担います。6章）。

---

## 2. Claude Code を導入する
使うには **Claude の有料プラン**（Pro / Max / Team / Enterprise）が必要です。

### おすすめ：デスクトップアプリ（ターミナル不要）
Claude デスクトップアプリを入れ、その中の **「Code」タブ**から使えます（macOS / Windows / Linux 対応）。画面操作だけで完結するので、非プログラマの方はこちらが簡単です。

### ターミナルで入れる場合（上級者向け）
公式のインストーラ（Node.js 不要）:
- **macOS / Linux / WSL**：`curl -fsSL https://claude.ai/install.sh | bash`
- **Windows PowerShell**：`irm https://claude.ai/install.ps1 | iex`
- 確認：`claude --version` ／ プロジェクトのフォルダで `claude` を実行して認証（ブラウザでログイン）。

<small>出典：Claude Code 公式ドキュメント（code.claude.com/docs/en/setup、docs.claude.com/en/docs/claude-code/setup）。ネイティブインストーラが推奨で、npm 版（`npm install -g @anthropic-ai/claude-code`、Node.js 22+ 必要）もあります。</small>

---

## 3. プロジェクトを開く
1. リポジトリを手元のフォルダに用意します（GitHub から ZIP をダウンロードして展開、またはクローン）。
   - Claude Code に **「editroom1980/footpath-map をクローンして」** と頼んでも構いません。
2. Claude Code で**そのフォルダを開く**と、`CLAUDE.md` が自動で読み込まれ、ルールを把握した状態で始まります。

---

## 4. 最初のセッション（動作確認）
Claude Code に、日本語でこう頼んでください。

1. 「**セットアップして。** `npm install` と、テスト用に Python の Playwright と Chromium を入れて」
2. 「**`npm test` を実行して、全項目 PASS になるか確認して**」

→ 全 PASS になれば準備完了です（＝今の状態を壊していない“基準線”）。
テスト環境（Python Playwright + Chromium）の初回セットアップは、Claude Code に任せられます。

---

## 5. 日々の使い方
1. 直したいことを**日本語で頼む**（例：「iPhone でも文字サイズを選べるようにして」）。
2. Claude Code が自動で：`index.html` を編集 → `APP_VERSION` を1つ上げ → **`npm test` で全 PASS を確認** → 変更をコミット。
3. **本番公開は、あなたの指示があるまで行いません**（`CLAUDE.md` で規定）。
   公開してよければ「**GitHub に反映して（push）**」と頼みます。push すると GitHub Pages が自動更新されます。
4. 公開後、URL の `?v=` を新しい版数へ上げてキャッシュ更新（`APP_VERSION` と一致させる）。

---

## 6. 元に戻す（ロールバック）
Git が全変更履歴を持つので、これまでの `index_v*_backup.html` は不要です。

- 直前に戻したい：「**一つ前のコミットに戻して**」と頼む（Claude Code が `git revert` などで対応）。
- 特定の版に戻したい：「**v78 の状態に戻して**」のように頼む。

---

## 覚えておくこと（サンドボックスと同じ制約）
- **保存画像の最終的な見え方・実機のタッチ感**は、テストでは完全に再現できません。**実機や公開 URL で最終確認**してください。
- 追加した背景地図（地形図・淡色・色違い）は外部サーバー依存です。読み込み速度は環境次第です。
- 迷ったら、`HANDOFF_開発引き継ぎ書.md` と `ARCHITECTURE.md` を Claude Code に読ませてください。
