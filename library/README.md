# みんなのコース

アプリの「配る → みんなのコースに出す」から出したコースが、このフォルダに 1 件 1 ファイル（JSON）で入ります。
中身は `{name, area, by, at, allowEdit, d}`。`d` はコースそのもの（写真なし・圧縮）です。

- 出し方：アプリで「配る」→「みんなのコースに出す」→ 名前と「見た人にできること」を選んで「出す」→ 開いた画面で緑の **Commit changes** を押す。
- 取り下げ：このフォルダのファイルを削除すれば、一覧から消えます。
